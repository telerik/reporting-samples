(function (trv, $) {
    "use strict";

    var sr = {
        invalidParameter: 'Ingrese un valor válido',
        parameterEditorSelectNone: 'Selección clara',
    };
    trv.sr = $.extend(trv.sr, sr);
}(window.telerikReportViewer = window.telerikReportViewer || {}, jQuery));